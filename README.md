# txch-faucet

#### Node

The ```node``` directory contains python script that should be setup as Scheduled tasks on the testnet node computer.

#### Web

The ```web``` directory contains the PHP code that runs the online website for requesting TXCH from the faucet.
